import { FilterCompaniesPipePipe } from './filter-companies-pipe.pipe';

describe('FilterCompaniesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCompaniesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
